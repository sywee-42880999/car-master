name: CAR MASTER production preview

on:
  push:
    branches: [main]
    paths:
      - "tools/build_production_preview.py"
      - ".github/workflows/production-preview.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python tools/build_production_preview.py
      - name: Commit warning DOM discovery
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research/warning-dom-discovery.md
          if git diff --cached --quiet; then
            echo "No discovery changes."
          else
            git commit -m "Inspect Hyundai warning icon DOM structure"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
